{
  "publisher": "maxbublik",
  "name": "vscode-stop-debug-and-terminate-tasks",
  "version": "0.0.1",
  "main": "dist/extension/main.js",
  "engines": {
    "vscode": "^1.60.0"
  },
  "contributes": {
    "commands": [
      {
        "command": "vscode-stop-debug-and-terminate-tasks",
        "title": "Stop debug and Terminate all tasks"
      }
    ]
  },
  "activationEvents": [
    "onCommand:vscode-stop-debug-and-terminate-tasks"
  ],
  "scripts": {
    "build": "rm -rf ./dist && mkdir -p ./dist/extension && cp ./src/extension.js ./dist/extension/main.js"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/maxbublik/vscode-stop-debug-and-terminate-tasks.git"
  }
}
